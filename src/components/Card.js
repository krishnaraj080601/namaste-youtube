import React from 'react'

const Card = ({info}) => {
    console.log(info);
   //const { snippet, statistics} = info;
    //const{ channelTitle,title, thumbnails } = snippet;

  return (

    <div>
    
    </div>
  );
};

export default Card;